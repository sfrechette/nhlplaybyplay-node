{
  "name": "nhlplaybyplay-node",
  "version": "1.1.0",
  "description": "Application built using Node.js that provides the means for accessing, fetching NHL Schedule and Play by Play game data in JSON format and converting the output to CSV.",
  "main": "fetch.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stéphane Fréchette",
  "license": "ISC",
  "dependencies": {
    "async": "^2.1.2",
    "colors": "^1.1.2",
    "jsonfile": "^2.4.0",
    "request": "^2.75.0"
  }
}
